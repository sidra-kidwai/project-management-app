<% if @time_log.errors.any? %>
  $("#errors").append("<%= j render 'shared/error_messages', object: @time_log %>");
<% else %>
  $('#timelog_modal').modal('hide');
  $(".time-log-<%= @time_log.id %>").html("<%= @time_log.hours_spent %>");
<% end %>
